{
  "name": "crashquote-server",
  "version": "1.0.0",
  "description": "Backend for CrashQuote AI — securely proxies Claude API and serves the app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.3"
  }
}{
  "name": "crashquote-server",
  "version": "1.0.0",
  "description": "Backend for CrashQuote AI — securely proxies Claude API and serves the app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
